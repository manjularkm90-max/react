async function execute(tasks, limit){

let running=[];

for(const task of tasks){

const p = task();

running.push(p);

if(running.length>=limit){

await Promise.race(running);

running = running.filter(x=>x!==p);

}

}

await Promise.all(running);

}
